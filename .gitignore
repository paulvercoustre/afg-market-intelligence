# Virtual environments
mrkt_intel_venv/
venv/
env/
ENV/

# Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
analysis.log

# Temporary files
*.tmp
*.temp

# Jupyter notebooks (if any)
.ipynb_checkpoints/

# Environment variables
.env
.env.local# Data files (for privacy/security)
raw_*.csv
output/raw_*.csv# API keys and secrets
secrets.json
config/secrets.json

# Build files
dist/
build/

# ETL logs
etl_run.log

# ETL disk caches (local speedup only, safe to delete/regenerate)
etl/.cache/

# Node / frontend
frontend/node_modules/
frontend/.next/
frontend/out/

# Alembic (don't ignore versions — they're source)

# Third-party copyrighted reprint -- Springer Nature's terms prohibit
# uploading their journal content to a repository (dicl.csv itself is fine,
# it's the underlying data, published separately by Harvard Dataverse under
# CC BY-NC-ND). See reference/build_language_reference.py for the citation.
reference/A_Dataset_on_Linguistic_Connectivity_Across_and_Wi.pdf

# Third-party copyrighted reference PDF -- same reasoning as above, kept
# locally for reference only, not re-hosted in the repo.
docs/OECD Handbook on Constructing Composite Indicators.pdf

# Local working file, not meant for the repo.
docs/INDICATOR_INVENTORY.xlsx

